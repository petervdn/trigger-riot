import { IGridItem } from '../data/interface';

export function getSlotsInRange(gridItem: IGridItem[], startTime: number, endTime: number): any[] {
  // get last one that starts before starttme
  return [];
}
